#define indef

#include <iostream>

class Address() {
  public:
    string[] street;
    string[] city;
    string[] state;
    string[] zipcode;
  private:
   void Address(Construtor -> string[]) {
	   return street, city, state, zipcode -> string[]
    }

  void Constructor(city -> string[], state -> string[], zipcode -> const static int, street -> string[]){
	 return Constructor()
    }

  return 0;

} 
